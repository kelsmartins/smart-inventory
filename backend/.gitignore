# Dependências e Ambiente Virtual
# Não enviamos a venv pois ela deve ser recriada em cada ambiente via requirements.txt
venv/
__pycache__/
*.pyc

# Segurança e Configurações Locais
# O arquivo .env contém chaves secretas e senhas do banco de dados (NUNCA subir ao Git)
.env

# Banco de Dados e Migrações
# A pasta instance contém o banco SQLite local. Migrations são controladas via Alembic.
migrations/
instance/

# Cache e Sistema
.pytest_cache/
.DS_Store